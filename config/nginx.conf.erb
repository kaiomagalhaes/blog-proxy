worker_processes  2;
error_log /dev/stdout info;

daemon off;

events {
	worker_connections 256;
}

http {
	access_log /dev/stdout;
	server {
		listen <%= ENV["PORT"] %>;
		server_name prod-website-proxy.herokuapp.com;


		location / {
			proxy_pass http://codelitt.com;
		}
		
		location /blog {
			proxy_pass http://blog.codelitt.dev;	
		}
	}
}